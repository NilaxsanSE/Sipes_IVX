# Docker

Docker support files for SIPES IVX.

The main orchestration file is at the repository root:

```bash
docker compose up --build
```

Application images are defined in:

- `frontend/Dockerfile`
- `backend/Dockerfile`
